/**
 * Batch engines that precompute the snapshot tables the screens read.
 *
 * <p>Application module. Types in this package root are the public API other
 * modules may depend on; everything under it is internal.
 */
@org.springframework.modulith.ApplicationModule(displayName = "engine")
package com.aatlas.engine;
