package com.aatlas.buy;

/** What matters most, as five weights that sum to 1. The frontend's {@code Weights}. */
public record Weights(double cost, double speed, double reliability, double risk, double relationship) {
}
